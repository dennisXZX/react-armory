import React from 'react';
import styles from './Card.module.scss';

const Card = () => {
    return (
        <section className={styles.card}>
            card
        </section>
    );
};

export default Card;
